<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Asignar Especialidad a Médico</title>
    <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        :root {
            --primary: #461414;
            --primary-light: #dbeafe;
            --primary-dark: #471717;
            --secondary: #10b981;
            --accent: #f43f5e;
            --warning: #f59e0b;
            --caution: #facc15;
            --danger: #e63946;
            --light: #f8fafc;
            --dark: #0f172a;
            --gray: #64748b;
            --light-gray: #e2e8f0;
            --card-bg: #ffffff;
            --border-radius: 16px;
            --shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06);
            --shadow-lg: 0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2px rgba(0, 0, 0, 0.05);
            --transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
        }

        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
        }

        body {
            font-family: 'Plus Jakarta Sans', sans-serif;
            background-color: #f1f5f9;
            color: var(--dark);
            line-height: 1.6;
            display: flex;
            flex-direction: column;
            min-height: 100vh;
            padding: 0;
        }

        /* Menú mejorado */
        #navbar {
            background-color: var(--card-bg);
            padding: 0 40px;
            display: flex;
            align-items: center;
            justify-content: space-between;
            box-shadow: var(--shadow);
            position: sticky;
            top: 0;
            z-index: 1000;
            min-height: 80px;
            border-bottom: 1px solid var(--light-gray);
            width: 100%;
        }

        #navbar nav {
            display: flex;
            justify-content: flex-end;
            align-items: center;
            width: 100%;
        }
        
        #navbar #menu {
            list-style: none;
            padding: 0;
            margin: 0;
            display: flex;
            gap: 10px;
            flex-wrap: wrap;
            justify-content: flex-end;
        }
        
        #navbar #menu li {
            margin-left: 0;
        }
        
        #navbar #menu a {
            color: var(--dark);
            text-decoration: none;
            padding: 10px 15px;
            border-radius: var(--border-radius);
            transition: var(--transition);
            font-weight: 500;
            display: flex;
            align-items: center;
            gap: 8px;
            font-size: 14px;
            white-space: nowrap;
        }
        
        #navbar #menu a:hover {
            background-color: var(--primary-light);
            color: var(--primary);
        }

        .logo {
            font-weight: 700;
            font-size: 1.4rem;
            color: var(--primary);
            display: flex;
            align-items: center;
            gap: 10px;
            padding: 10px 0;
        }

        .logo i {
            font-size: 1.6rem;
        }

        .main-container {
            max-width: 1000px;
            width: 100%;
            margin: 40px auto;
            padding: 0 20px;
            flex: 1;
        }

        .form-container {
            background: var(--card-bg);
            padding: 40px;
            border-radius: var(--border-radius);
            box-shadow: var(--shadow);
            border: 1px solid var(--light-gray);
            margin-bottom: 30px;
        }

        h1, h2 {
            font-size: 2rem;
            font-weight: 700;
            margin-bottom: 20px;
            color: var(--dark);
            display: flex;
            align-items: center;
            gap: 12px;
        }

        h1 i, h2 i {
            color: var(--primary);
        }

        label {
            display: block;
            margin-top: 20px;
            font-weight: 600;
            color: var(--dark);
        }
        
        input[type="text"],
        input[type="number"],
        select {
            width: 100%;
            padding: 12px 16px;
            margin: 8px 0 20px 0;
            border: 1px solid var(--light-gray);
            border-radius: var(--border-radius);
            box-sizing: border-box;
            font-size: 16px;
            font-family: 'Plus Jakarta Sans', sans-serif;
            transition: var(--transition);
            background-color: var(--light);
        }
        
        input[type="text"]:focus,
        input[type="number"]:focus,
        select:focus {
            border-color: var(--primary);
            outline: none;
            box-shadow: 0 0 0 3px rgba(70, 20, 20, 0.1);
        }

        .btn {
            display: inline-flex;
            align-items: center;
            gap: 8px;
            background-color: var(--primary);
            color: white;
            padding: 12px 24px;
            border-radius: var(--border-radius);
            text-decoration: none;
            font-weight: 500;
            transition: var(--transition);
            border: none;
            cursor: pointer;
            font-size: 15px;
            border: 1px solid transparent;
            margin-top: 20px;
        }
        
        .btn:hover {
            background-color: var(--primary-dark);
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(70, 20, 20, 0.3);
        }

        .btn-secondary {
            background-color: var(--gray);
        }

        .btn-secondary:hover {
            background-color: #475569;
        }

        .btn-group {
            display: flex;
            gap: 12px;
            margin-bottom: 30px;
            flex-wrap: wrap;
        }

        /* Select2 styles adapted */
        .select2-container {
            width: 100% !important;
            margin: 8px 0 20px 0 !important;
        }
        
        .select2-container .select2-selection--single {
            height: 48px !important;
            border: 1px solid var(--light-gray) !important;
            border-radius: var(--border-radius) !important;
            padding: 0 35px 0 15px !important;
            display: flex !important;
            align-items: center !important;
            box-sizing: border-box !important;
            background-color: var(--light) !important;
            transition: var(--transition) !important;
        }
        
        .select2-container .select2-selection__rendered {
            padding: 0 !important;
            line-height: normal !important;
            color: var(--dark) !important;
            font-size: 16px !important;
            font-family: 'Plus Jakarta Sans', sans-serif !important;
        }

        .select2-container .select2-selection__arrow {
            height: 100% !important;
            width: 25px !important;
            right: 7px !important;
            top: 0 !important;
            display: flex !important;
            align-items: center !important;
            justify-content: center !important;
        }

        .select2-dropdown {
            border: 1px solid var(--light-gray) !important;
            border-radius: var(--border-radius) !important;
            box-shadow: var(--shadow-lg) !important;
            background-color: var(--card-bg) !important;
        }

        .select2-results__option {
            padding: 10px 15px !important;
            font-family: 'Plus Jakarta Sans', sans-serif !important;
        }
        
        .select2-results__option--highlighted:not(.select2-results__option--disabled) {
            background-color: var(--primary) !important;
            color: white !important;
        }

        .select2-selection__placeholder {
            color: var(--gray) !important;
        }

        /* Responsive */
        @media (max-width: 768px) {
            .form-container {
                padding: 30px;
            }
            
            .btn-group {
                flex-direction: column;
            }
        }

        @media (max-width: 480px) {
            .form-container {
                padding: 20px;
            }
            
            h1 {
                font-size: 1.6rem;
            }
            
            .btn {
                padding: 10px 16px;
                font-size: 14px;
                width: 100%;
                justify-content: center;
            }
        }
    </style>

    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.7.1/jquery.min.js"></script>
    <link href="https://cdn.jsdelivr.net/npm/select2@4.1.0-rc.0/dist/css/select2.min.css" rel="stylesheet" />
    <script src="https://cdn.jsdelivr.net/npm/select2@4.1.0-rc.0/dist/js/select2.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/select2@4.1.0-rc.0/dist/js/i18n/es.js"></script> 
</head>
<body>
    <div id="navbar">
        <div class="logo">
            <i class="fas fa-heartbeat"></i>
            <span>BioMed</span>
        </div>
        <nav>
            <ul id="menu">
                <!-- Se llenará dinámicamente con JavaScript -->
            </ul>
        </nav>
    </div>

    <div class="main-container">
        <div class="form-container">
            <h1><i class="fas fa-user-md"></i> Asignar/Actualizar Especialidad de Médico</h1>
            <form action='/asignar-especialidad' method="POST"> 
                <label for="medico_id">Seleccionar Médico:</label>
                <select id="medico_id" name="medico_id" required>
                    <option value="">Selecciona un médico</option>
                    <% if (medicos && medicos.length > 0) { %>
                        <% medicos.forEach(medico => { %>
                            <option value="<%= medico.id %>">
                                <%= medico.nombre %> <% if (medico.especialidad) { %> (Actual: <%= medico.especialidad %>) <% } %>
                            </option>
                        <% }); %>
                    <% } else { %>
                        <option value="" disabled>No hay médicos disponibles para asignarles especialidad.</option>
                    <% } %>
                </select>

                <label for="especialidad">Nueva Especialidad:</label>
                <select id="especialidad" name="especialidad" required>
                    <option value="">Selecciona una especialidad</option>
                    <option value="Cardiología">Cardiología</option>
                    <option value="Dermatología">Dermatología</option>
                    <option value="Pediatría">Pediatría</option>
                    <option value="Neurología">Neurología</option>
                    <option value="Cirugía General">Cirugía General</option>
                    <option value="Medicina Interna">Medicina Interna</option>
                    <option value="Oftalmología">Oftalmología</option>
                    <option value="Ginecología y Obstetricia">Ginecología y Obstetricia</option>
                    <option value="Ortopedia y Traumatología">Ortopedia y Traumatología</option>
                    <option value="Psiquiatría">Psiquiatría</option>
                    <option value="Endocrinología">Endocrinología</option>
                    <option value="Gastroenterología">Gastroenterología</option>
                    <option value="Urología">Urología</option>
                    <option value="Anestesiología">Anestesiología</option>
                    <option value="Radiología">Radiología</option>
                </select>

                <button type="submit" class="btn"><i class="fas fa-save"></i> Actualizar Especialidad</button>
            </form>
        </div>

        <div class="btn-group">
            <button onclick="window.location.href='/medicos'" class="btn"><i class="fas fa-users"></i> Ver Todos los Médicos</button>
            <div style="display: flex; flex-direction: column; gap: 10px;">
    <button onclick="window.location.href='/ordenar-medicos'" class="btn btn-secondary">
        <i class="fas fa-sort"></i> Ordenar por Especialidad
    </button>
    <button onclick="window.location.href='/'" class="btn">
        <i class="fas fa-arrow-left"></i> Regresar al Inicio
    </button>
</div>
        </button>
        </div>
    </div>

    <script>
        document.addEventListener('DOMContentLoaded', function() {
            $('#medico_id').select2({
                dropdownParent: $('body'),
                placeholder: "Selecciona un médico",
                allowClear: true,
                language: "es"
            });
            $('#especialidad').select2({
                dropdownParent: $('body'),
                placeholder: "Selecciona una especialidad",
                allowClear: true,
                language: "es"
            });
        });
    </script>
</body>
</html>
